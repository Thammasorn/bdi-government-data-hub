-- หนึ่งผู้ใช้ = หนึ่งบทบาท (ตัดสินใจ 2026-09-03)
--
-- กฎนี้บังคับที่ `assignRole()` ใน src/lib/iam.ts เป็นหลัก เพราะที่นั่นตอบกลับเป็น 409
-- พร้อมบอกว่าบทบาทที่ถืออยู่คืออะไรและต้องไปทางไหนต่อได้ index ตัวนี้คือตาข่ายชั้นล่าง
-- ที่รับสิ่งที่โค้ดรับไม่ได้: การเขียนพร้อมกันสองเส้นทาง (เปิดใช้งานบัญชีจาก activation key
-- กับ POST /api/admin/users/:id/roles มาถึงในวินาทีเดียวกัน) ซึ่ง `assignRole()` มองไม่เห็น
--
-- เขียนได้จริงต่างจาก `uq_active_org_scoped_role_assignment` ที่ถูกลบไปเมื่อ 2026-08-16
-- เพราะกฎนี้ไม่ต้องแยกว่าเป็น role ไหน จึงไม่ต้องอ้าง `role_id` ที่สุ่มใหม่ทุกฐานข้อมูล
-- และไม่มีข้อยกเว้นให้หน่วยงาน BDI — เจ้าหน้าที่ BDI ก็ถือได้บทบาทเดียวเหมือนกัน
--
-- เงื่อนไขที่นี่คือ `status = 'ACTIVE'` เท่านั้น ส่วน `activeAssignmentWhere()` ในโค้ด
-- นับ `effective_until` ที่เลยมาแล้วเป็น EXPIRED ด้วย เขียนลง index ไม่ได้เพราะต้องเรียก
-- now() ซึ่งไม่ immutable ทุกวันนี้ไม่มีโค้ดไหนเขียนค่า `effective_until` เลย (อ่านอย่างเดียว)
-- ทั้งสองจึงตรงกันพอดี — วันที่เริ่มใช้ค่านั้น index จะเข้มกว่ากฎจริง และต้องกลับมาคิดใหม่ตรงนี้
CREATE UNIQUE INDEX "uq_one_active_role_per_user"
  ON "iam"."user_role_assignment" ("user_account_id")
  WHERE "status" = 'ACTIVE';
