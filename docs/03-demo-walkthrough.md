# คู่มือเดินระบบทีละขั้น (Demo Walkthrough)

เอกสารนี้ใช้เมื่อต้องการ **สาธิตระบบ** หรือ **ทดสอบซ้ำตั้งแต่ต้น** ทำตามได้ทีละบรรทัด
ไม่ต้องเดา ครอบคลุมทั้งสองเส้นทางใน [`01-user-journey.md`](01-user-journey.md)

> ทุกคำสั่งในเอกสารนี้รันจากไดเรกทอรีของ checkout ที่คุณใช้ เช่น
> `cd /hdd1tb/bdi-project/main` แล้วค่อยรัน
>
> ถ้าเทอร์มินัลขึ้น `permission denied ... docker.sock` แปลว่า shell นั้นเปิดก่อนที่จะถูก
> เพิ่มเข้ากลุ่ม `docker` — เปิดเทอร์มินัลใหม่ หรือคั่นด้วย `newgrp docker`

---

## 0. ตารางอ้างอิงเร็ว

| checkout | หน้าเว็บ | API | ไฟล์ตั้งค่า |
| --- | --- | --- | --- |
| `main` | <http://localhost:3001> | <http://localhost:4000> | `main/.env` |
| `dev/dev_01` | <http://localhost:3110> | <http://localhost:4110> | `dev/dev_01/.env` |

พอร์ตของแต่ละ checkout ต่างกันโดยตั้งใจ — ดู [`../README.md`](../README.md) หัวข้อ
"Working alongside other developers"

---

## 1. เตรียมระบบให้พร้อม

### 1.1 สตาร์ตทุกบริการ

```bash
docker compose up -d --build
```

รอจนครบ แล้วตรวจว่าทุกอย่างเขียว:

```bash
docker compose ps
curl -s http://localhost:4000/health/ready
```

ต้องได้:

```json
{"status":"ok","checks":{"database":{"status":"up"},"storage":{"status":"up"}}}
```

ถ้า `status` เป็น `degraded` ให้ดูว่า `database` หรือ `storage` ตัวไหนล่ม แล้วดูหัวข้อ 7

### 1.2 สร้างตารางในฐานข้อมูล (ครั้งแรกเท่านั้น)

```bash
docker compose exec backend npx prisma migrate deploy
```

ถ้าขึ้น `No pending migrations` แปลว่าเคยรันไปแล้ว ข้ามได้

---

## 2. เลือกโหมดอีเมล

ระบบส่งอีเมล 7 แบบตามตารางใน [`01-user-journey.md` §4](01-user-journey.md)
เลือกได้ว่าจะให้ส่งจริงหรือแค่พิมพ์ลง log

### โหมด A — ไม่ส่งจริง (แนะนำตอนสาธิต)

ปล่อย `SMTP_USER` ใน `.env` ว่างไว้ ระบบจะพิมพ์เนื้ออีเมล **ลิงก์คำเชิญ** และ **รหัส OTP**
ลง log ให้แทน ไม่ต้องมีอีเมลจริง ไม่ต้องรอ inbox

เปิดหน้าต่างเทอร์มินัลไว้อีกอันเพื่อเฝ้า log:

```bash
docker compose logs -f backend | grep --line-buffered 'mail:dry-run'
```

จะเห็นแบบนี้ทุกครั้งที่ระบบ "ส่ง" อีเมล:

```
[mail:dry-run] ถึง: somchai@moph.go.th
[mail:dry-run] เรื่อง: คำเชิญเข้าใช้งาน Government Datahub Platform
[mail:dry-run] ลิงก์: http://localhost:3001/register?token=xxxxxxxx
[mail:dry-run] รหัส OTP: 048213
```

### โหมด B — ส่งจริงผ่าน Gmail

ตั้งค่าใน `.env` (ไฟล์นี้ถูก gitignore ไว้ ไม่ขึ้น repo):

```dotenv
SMTP_USER=บัญชี@gmail.com
SMTP_PASS=<App Password 16 หลัก ลบช่องว่างออก>
```

**ต้องเป็น App Password เท่านั้น** รหัสผ่าน Google ปกติใช้ไม่ได้:

1. เปิด 2-Step Verification ที่ <https://myaccount.google.com/security>
2. สร้าง App Password ที่ <https://myaccount.google.com/apppasswords>
   เลือก Mail → Other → ตั้งชื่อเช่น "BDI Datahub"
3. คัดลอกรหัส 16 หลัก **ลบช่องว่างออกให้หมด** แล้ววางใน `SMTP_PASS`

รีสตาร์ตให้ค่าใหม่มีผล:

```bash
docker compose up -d backend
```

ตรวจว่าเข้าโหมดส่งจริงแล้ว — ถ้ายังเห็นบรรทัดนี้ใน log แปลว่ายังไม่ติด:

```bash
docker compose logs backend | grep 'SMTP ยังไม่ได้ตั้งค่า'
```

ไม่มีผลลัพธ์ = ส่งจริงแล้ว

> อีเมลจาก Gmail มักตกโฟลเดอร์ **Spam/Promotions** ในการทดสอบครั้งแรก
> ถ้าไม่เห็นในกล่องหลักให้ดูที่นั่นก่อน

---

## 3. ใส่ข้อมูลตัวอย่าง

คำสั่งเดียวจบ — **ล้างข้อมูลเดิมทั้งหมดก่อนเสมอ** แล้วสร้างใหม่ รันซ้ำได้ไม่จำกัด:

```bash
docker compose exec backend npm run seed:demo
```

ได้อะไรมาบ้าง:

**หน่วยงาน 5 แห่ง ครบทุกสถานะ** แต่ละแห่งมี PDF ที่ระบบสร้างจริง ไฟล์แนบ และ timeline ย้อนหลัง

| หน่วยงาน | สถานะ | ใครทำต่อได้ |
| --- | --- | --- |
| สำนักงานสาธารณสุขจังหวัดเชียงใหม่ | รอตรวจสอบจาก BDI | `officer@bdi.or.th` |
| กรมที่ดิน | รอการแก้ไข | `malee@dol.go.th` (เจ้าของ) |
| สำนักงานพัฒนาสังคมฯ ขอนแก่น | รอตรวจสอบจากผู้มีอำนาจ | `wichai@cmpho.go.th` |
| กรมการปกครอง | รอ BDI ลงนาม | `approver@bdi.or.th` |
| สำนักงานสถิติแห่งชาติ | เปิดใช้งาน | — (จบแล้ว) |

**บัญชีผู้ใช้ — รหัสผ่านเดียวกันหมด: `bdi12345`**

| อีเมล | บทบาท | หมายเหตุ |
| --- | --- | --- |
| `officer@bdi.or.th` | เจ้าหน้าที่ BDI | เห็นทุกหน่วยงาน |
| `approver@bdi.or.th` | ผู้อนุมัติ BDI | ลงนามขั้นสุดท้าย |
| `wichai@cmpho.go.th` | ผู้มีอำนาจกระทำการแทน | เป็นผู้ลงนามของหลายหน่วยงาน |
| `somchai@moph.go.th` | ผู้ใช้หน่วยงาน | คำขออยู่ระหว่างตรวจสอบ |
| `malee@dol.go.th` | ผู้ใช้หน่วยงาน | ถูกส่งกลับให้แก้ไข |
| `newbie@moi.go.th` | ผู้ใช้หน่วยงาน | **ยังไม่มีหน่วยงาน — ใช้ดูหน้า empty state** |

---

## 4. เดิน Journey A — Admin เชิญผู้ใช้ใหม่

สเปกระบุว่าขั้นตอนนี้ **ไม่มี UI** จึงเรียก API ตรง

### ขั้นที่ 1 — ส่งคำเชิญ

```bash
source .env
curl -X POST http://localhost:4000/api/admin/invitations \
  -H "x-admin-token: $ADMIN_API_TOKEN" \
  -H 'Content-Type: application/json' \
  -d '{"email":"newperson@agency.go.th","role":"ORGANIZATION_USER"}'
```

`role` ใส่ได้: `BDI_OFFICER` · `BDI_APPROVER` · `BDI_SPECIALIST` ·
`ORGANIZATION_USER` · `ORGANIZATION_APPROVER`

ตอบกลับ:

```json
{"invitationId":"...","email":"newperson@agency.go.th","role":"ORGANIZATION_USER",
 "roleLabel":"ผู้ใช้จากหน่วยงาน","expiresAt":"..."}
```

> API จงใจ **ไม่คืน token** กลับมา — token อยู่ในอีเมลเท่านั้น
> ถ้าเชิญอีเมลเดิมซ้ำ คำเชิญอันเก่าจะถูกยกเลิกอัตโนมัติ เหลือใช้ได้อันเดียวเสมอ

### ขั้นที่ 2 — หาลิงก์สมัคร

**โหมด A (ไม่ส่งจริง)** — ดึงจาก log:

```bash
docker compose logs backend --tail 200 \
  | grep -A2 'ถึง: newperson@agency.go.th' | grep 'ลิงก์:'
```

**โหมด B (ส่งจริง)** — เปิดอีเมลแล้วกดปุ่ม "เริ่มลงทะเบียน"

### ขั้นที่ 3 — กรอกข้อมูลผู้ใช้

เปิดลิงก์ในเบราว์เซอร์ จะเห็นหน้า **ลงทะเบียนเข้าใช้งาน**

1. ช่อง **อีเมล** ล็อกไว้แก้ไม่ได้ (กันคำเชิญถูกใช้ผิดคน)
2. เลือก **คำนำหน้า** กรอก **ชื่อ / นามสกุล**
3. กรอก **เบอร์โทรศัพท์** — ต้องขึ้นต้น `0` และมี 9–10 หลัก
4. ตั้ง **รหัสผ่าน** — อย่างน้อย 8 ตัว มีทั้งตัวอักษรและตัวเลข
5. กด **ถัดไป — ยืนยันตัวตน**

ลองผิดดูได้: ใส่รหัสผ่านสั้น ๆ แล้วกดส่ง จะเห็น error ใต้ช่องพร้อม toast เตือน

### ขั้นที่ 4 — ยืนยันตัวตน (2FA)

มีสองทาง เลือกทางใดทางหนึ่ง

**ทาง 1 — รหัส OTP ทางอีเมล**

หารหัส 6 หลัก:

```bash
docker compose logs backend --tail 40 | grep 'รหัส OTP'
```

กรอกลงช่อง 6 กล่อง (วางทั้งชุดทีเดียวได้) ครบ 6 หลักระบบส่งให้อัตโนมัติ ไม่ต้องกดปุ่ม

- รหัสอายุ 10 นาที
- ผิดได้ 5 ครั้ง เกินนั้นต้องกดขอใหม่
- ปุ่มขอรหัสใหม่ต้องรอ 60 วินาที

**ทาง 2 — ThaiD (โหมดจำลอง)**

กดปุ่ม **ยืนยันด้วย ThaiD** ผ่านทันที ไม่ต้องหารหัส

ปุ่มนี้จะโผล่ก็ต่อเมื่อ `.env` ตั้ง `THAID_MOCK=true` เท่านั้น
ค่าเริ่มต้นเป็น `false` เพราะยังไม่มี OAuth client จริงของ ThaiD
**ห้ามเปิดบน production** เพราะจะข้ามการยืนยันตัวตนได้ทั้งหมด

### ผลลัพธ์

ระบบล็อกอินให้อัตโนมัติแล้วพาไปหน้าแรกตามบทบาท:

- เจ้าหน้าที่ BDI → `/admin/organizations`
- ผู้ใช้หน่วยงาน → `/` (หน้า empty state ถ้ายังไม่มีหน่วยงาน)

ลิงก์คำเชิญ **ใช้ได้ครั้งเดียว** เปิดซ้ำจะขึ้น "ลิงก์คำเชิญนี้ถูกใช้ไปแล้ว"

---

## 5. เดิน Journey B — สร้างหน่วยงานจนเปิดใช้งาน

เส้นทางนี้ต้องสลับบัญชี 4 ครั้ง แนะนำให้ใช้ **หน้าต่าง incognito แยก** สำหรับแต่ละบทบาท
จะได้ไม่ต้อง log out เข้าใหม่ตลอด

### ขั้นที่ 1 — ผู้ใช้หน่วยงานกรอกและนำส่ง

เข้าสู่ระบบด้วย `newbie@moi.go.th` / `bdi12345`

1. หน้าแรกมี **ปุ่มเดียวกลางจอ**: `สร้างหน่วยงาน` (ตามสเปก เมนูอื่นถูกซ่อนหมด) — กด
2. เข้าหน้าฟอร์ม ซ้ายมือเป็น stepper 3 ขั้นบอกว่ากรอกครบหรือยัง

**ส่วนที่ 1 — ข้อมูลหน่วยงาน**
- ชื่อหน่วยงาน, ที่อยู่
- **จังหวัด → อำเภอ → ตำบล** เป็น dropdown ลูกโซ่ ต้องเลือกไล่ตามลำดับ
  (เปลี่ยนจังหวัดแล้วอำเภอกับตำบลจะถูกล้าง เพื่อไม่ให้ได้ที่อยู่ที่ไม่มีจริง)
- **รหัสไปรษณีย์เติมให้อัตโนมัติ** เมื่อเลือกตำบลครบ
- อีเมลหน่วยงาน

**ส่วนที่ 2 — ผู้มีอำนาจกระทำการแทน**
- ชื่อ ตำแหน่ง อีเมล เบอร์โทร
- **เลขบัตรประชาชน 13 หลัก** — ระบบตรวจ checksum จริง ใส่มั่วไม่ผ่าน
  เลขที่ผ่านสำหรับทดสอบ: `1101700207994`
- **แนบคำสั่งแต่งตั้ง** (บังคับ) — PDF หรือ JPG ไม่เกิน 10 MB ลากมาวางได้
- แนบคำสั่งมอบอำนาจ (ถ้ามี)

> **อีเมลในส่วนที่ 2 สำคัญมาก** ระบบจะส่งคำขอให้ลงนามไปที่อีเมลนี้ในขั้นที่ 3
> ถ้าอยากให้ `wichai@cmpho.go.th` เป็นคนเห็นชอบ ให้ใส่อีเมลนี้

**ส่วนที่ 3 — ผู้กรอกข้อมูล**
- ชื่อ ตำแหน่ง ฝ่าย/กอง/สำนัก อีเมล เบอร์โทร

3. ลองกด **บันทึกแบบร่าง** ระหว่างทาง — ปิดเบราว์เซอร์แล้วกลับมาข้อมูลยังอยู่
4. กด **ตรวจสอบและสร้าง PDF**
   - ถ้ากรอกไม่ครบ จะขึ้น **toast เตือน** พร้อมเลื่อนจอไปที่ช่องแรกที่ผิด
   - ถ้าผ่าน ระบบสร้าง PDF จาก template แล้วพาไปหน้าตรวจสอบ
5. ดู PDF ที่ระบบสร้าง (ฝังในหน้าเลย เปิดแท็บใหม่ได้)
6. กด **นำส่งฟอร์มสร้างหน่วยงาน**

**เกิดอะไรขึ้น:** สถานะเปลี่ยนเป็น `รอตรวจสอบจาก BDI` และระบบส่งอีเมลแจ้ง
เจ้าหน้าที่ BDI **ทุกคน** แล้ว redirect มาหน้ารายละเอียดพร้อม timeline

### ขั้นที่ 2 — เจ้าหน้าที่ BDI ตรวจสอบ

เข้าสู่ระบบด้วย `officer@bdi.or.th` / `bdi12345`

1. เห็นตารางหน่วยงาน มีคอลัมน์ ชื่อหน่วยงาน · ผู้สร้าง+อีเมล · สถานะ · วันที่ยื่น
2. กรองด้วยชิปสถานะ หรือค้นหาจากชื่อ/ผู้ยื่นได้
3. คลิกแถวที่สถานะ `รอตรวจสอบจาก BDI`
4. ด้านบนมีการ์ด **"รอการพิจารณาของคุณ"** พร้อมสองปุ่ม

**ทางที่ 1 — ส่งกลับให้แก้**

กด `ต้องปรับปรุง` → modal เปิดขึ้น → พิมพ์สิ่งที่ต้องแก้ (**บังคับอย่างน้อย 10 ตัวอักษร**)
→ กดยืนยัน

สถานะเป็น `รอการแก้ไข` และส่งอีเมลพร้อมข้อความที่พิมพ์ไปหาผู้กรอก
กลับไปล็อกอินเป็นผู้ใช้หน่วยงานจะเห็นกล่องแดงบอกเหตุผล พร้อมปุ่มแก้ไข
แก้แล้วนำส่งใหม่ → กลับมาที่ `รอตรวจสอบจาก BDI`

**ทางที่ 2 — อนุมัติ**

กด `อนุมัติ` → ยืนยันใน modal

สถานะเป็น `รอตรวจสอบจากผู้มีอำนาจ` และระบบจะ:
- ถ้าอีเมลผู้มีอำนาจ **ยังไม่มีบัญชี** → สร้างคำเชิญให้อัตโนมัติ แล้วส่งลิงก์สมัคร
  (คนนั้นต้องไปเดิน Journey A ก่อน)
- ถ้ามีบัญชีแล้ว → เพิ่มสิทธิ์ให้ทันที แล้วส่งอีเมลแจ้งไปตรวจสอบ

### ขั้นที่ 3 — ผู้มีอำนาจกระทำการแทนเห็นชอบ

เข้าสู่ระบบด้วยอีเมลที่กรอกไว้ในส่วนที่ 2 (เช่น `wichai@cmpho.go.th` / `bdi12345`)

1. เปิดหน้ารายละเอียดหน่วยงานนั้น
2. รีวิว PDF
3. กด `เห็นชอบ` → สถานะเป็น `รอ BDI ลงนาม` และแจ้งผู้อนุมัติ BDI
   (หรือ `ต้องปรับปรุง` เพื่อส่งกลับ)

### ขั้นที่ 4 — ผู้อนุมัติ BDI ลงนาม

เข้าสู่ระบบด้วย `approver@bdi.or.th` / `bdi12345`

1. เปิดรายการที่สถานะ `รอ BDI ลงนาม`
2. กด `เห็นชอบและลงนาม`

**สถานะเป็น `เปิดใช้งาน`** ระบบแจ้งทั้งผู้กรอกและผู้มีอำนาจว่าหน่วยงานพร้อมใช้งานแล้ว

### ตรวจผลงาน

เปิดหน้ารายละเอียดหน่วยงาน เลื่อนลงล่างสุดจะเห็น **ประวัติการดำเนินการ** ครบทุกขั้น
ว่าใครทำอะไรเมื่อไหร่ พร้อมข้อความที่พิมพ์ตอนส่งกลับแก้

---

## 6. เริ่มใหม่ทั้งหมด

### ล้างแค่ข้อมูล (เก็บ container ไว้)

```bash
docker compose exec backend npm run seed:demo
```

### ล้างฐานข้อมูลให้ว่างเปล่าจริง ๆ

```bash
docker compose exec postgres psql -U bdi -d bdi -c \
  'TRUNCATE organization_events, attachments, organizations, invitations, otp_codes, users CASCADE;'
```

จากนั้นเชิญผู้ใช้คนแรกตามหัวข้อ 4

### รื้อทุกอย่างรวมถึงไฟล์ที่อัปโหลด

```bash
docker compose down -v          # ลบ container + volume ทั้งหมดของ checkout นี้
docker compose up -d --build
docker compose exec backend npx prisma migrate deploy
docker compose exec backend npm run seed:demo
```

`down -v` ลบเฉพาะ volume ของ `COMPOSE_PROJECT_NAME` ตัวเอง ไม่กระทบ checkout ของคนอื่น

---

## 7. แก้ปัญหาที่เจอบ่อย

| อาการ | สาเหตุ | วิธีแก้ |
| --- | --- | --- |
| `no configuration file provided: not found` | รันจากไดเรกทอรีที่ไม่มี `docker-compose.yml` | `cd` เข้า checkout ก่อน |
| `permission denied ... docker.sock` | shell เปิดก่อนถูกเพิ่มเข้ากลุ่ม `docker` | เปิดเทอร์มินัลใหม่ |
| Backend ขึ้น `ERR_MODULE_NOT_FOUND` | เพิ่ง pull โค้ดที่มี dependency ใหม่ | `docker compose up -d --build backend` (คอนเทนเนอร์รัน `npm install` ตอนสตาร์ตอยู่แล้ว) |
| หน้าเว็บ 500 หลัง pull | cache `.next` ค้างจาก build เก่า | `docker compose rm -sf frontend && docker volume rm <project>_frontend-next-cache && docker compose up -d frontend` |
| ไม่ได้รับอีเมล | ยังอยู่โหมด dry-run | ดู log ตามหัวข้อ 2 |
| Gmail ตอบ `EAUTH` | ใช้รหัสผ่านปกติแทน App Password หรือมีช่องว่างติดมา | สร้าง App Password ใหม่ ลบช่องว่างออก |
| `ตั้งค่า JWT_SECRET ใน .env ก่อน` | `.env` ไม่มีตัวแปรที่จำเป็น | คัดลอกจาก `.env.example` มาเติม |
| ปุ่ม ThaiD ไม่โผล่ | `THAID_MOCK` ไม่ได้เป็น `true` | แก้ `.env` แล้ว `docker compose up -d backend frontend` |
| พอร์ตชน | มีสแตกอื่นใช้พอร์ตเดียวกัน | แก้พอร์ตใน `.env` — port 3000 ถูก `open-webui` ใช้อยู่ถาวร |

### ดูข้อมูลในฐานข้อมูลตรง ๆ

```bash
docker compose exec backend npm run prisma:studio   # เปิด GUI
docker compose exec postgres psql -U bdi -d bdi     # เปิด psql
```

### ดูไฟล์ที่อัปโหลด

เปิด MinIO console — `main` อยู่ที่ <http://localhost:9001>
ล็อกอินด้วยค่าใน `.env` (`MINIO_ROOT_USER` / `MINIO_ROOT_PASSWORD`)
ไฟล์อยู่ใน bucket `bdi-uploads` ใต้ `organizations/<id>/`

---

## 8. เช็กลิสต์สำหรับสาธิตให้คนอื่นดู

ลำดับที่เล่าได้ลื่นที่สุดใน 10 นาที:

1. `docker compose exec backend npm run seed:demo` — เตรียมข้อมูลสด
2. เปิด log dry-run ค้างไว้อีกหน้าต่าง (ถ้าไม่ได้ตั้ง SMTP จริง)
3. ล็อกอิน `newbie@moi.go.th` → โชว์ **หน้า empty state** ที่มีปุ่มเดียว
4. กดสร้างหน่วยงาน → กรอกส่วนที่ 1 → โชว์ **dropdown ที่อยู่ลูกโซ่ + เติมรหัสไปรษณีย์ให้**
5. กรอกเลขบัตรมั่ว → กดสร้าง PDF → โชว์ **toast เตือนและการเลื่อนไปช่องที่ผิด**
6. แก้เป็น `1101700207994` แนบไฟล์ → สร้าง PDF → **โชว์ PDF ภาษาไทยที่ระบบ generate**
7. นำส่ง → โชว์ **อีเมลแจ้งเจ้าหน้าที่** ใน log
8. สลับเป็น `officer@bdi.or.th` → กด `ต้องปรับปรุง` → โชว์ modal และอีเมลที่ส่งกลับ
9. สลับกลับเป็นผู้ใช้ → โชว์กล่องแดงบอกสิ่งที่ต้องแก้ → แก้แล้วส่งใหม่
10. `officer` อนุมัติ → `wichai@cmpho.go.th` เห็นชอบ → `approver@bdi.or.th` ลงนาม
11. จบด้วยการโชว์ **timeline** ที่บันทึกครบทุกขั้น
