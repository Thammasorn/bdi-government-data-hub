# แบบฟอร์มลงทะเบียน Metadata ของชุดข้อมูล

การ์ด Notion: **Metadata Registration Form**
แหล่งอ้างอิงเดียวของเอกสารฉบับนี้คือ `assets/metadata_registration_form/metadata_mapping.xlsx`
และ `assets/db_schema/draft_db_design_downloaded_on_2026-08-16.xlsx`
เมื่อสองไฟล์นั้นเปลี่ยน เอกสารนี้กับโค้ดต้องเปลี่ยนตาม ไม่ใช่ทางกลับกัน

---

## 1. ไฟล์ต้นทางและหน้าที่ของแต่ละชีท

| ชีท | ใช้ทำอะไร | ลงเอยที่ไหนในโค้ด |
| --- | --- | --- |
| `A4_dataset_metadata` | รายการช่องที่ผู้ใช้ต้องกรอก (display) คู่กับคอลัมน์ที่เก็บผล (field_name) และรายการรหัสของแต่ละช่อง | ป้ายภาษาไทย + รายการรหัสใน `backend/src/lib/dataset.ts` และสำเนาใน `frontend/lib/dataset-form.ts` |
| `conditions` | เลือกอะไรแล้วช่องไหนเปิด ปิด หรือถูกบังคับค่า | `metadataRules()` / `normaliseMetadata()` (backend) และ `formRules()` / `applyRules()` (frontend) |
| `สัญญาอนุญาต` | รายการสัญญาอนุญาต และเหตุผลที่ตัด G1/G3/G4 ออก | `LICENSE_LABELS` |
| `DGA_GDCat_2106` | ตารางอ้างอิงของ DGA ไม่ได้ใช้ตรง ๆ | — |

**คำว่า “default” ในชีท `conditions` แปลว่า “บังคับ”** ไม่ใช่ค่าตั้งต้นที่ผู้ใช้เปลี่ยนได้
(การ์ดระบุไว้ชัด) แต่หมายเหตุท้ายชีทก็สั่งด้วยว่า *ค่าที่ถูกบังคับต้องแสดงบนหน้าจอให้ผู้ใช้เห็น*
ฟอร์มจึงแสดงช่องนั้นพร้อมค่าและเหตุผล แล้วปิดไม่ให้แก้ ไม่ใช่ซ่อนทิ้ง

---

## 2. ฐานข้อมูล

`dataset.dataset_registration_metadata` (ระหว่างร่างและระหว่างตรวจ) และ
`dataset.dataset_metadata` (เมื่อชุดข้อมูลเข้าระบบแล้ว) **เปลี่ยนชุดคอลัมน์ทั้งตาราง**
จากฉบับ 2026-08-11 มาเป็นฉบับ 2026-08-16 — 35 คอลัมน์ ชื่อตรงกับ field_name ในชีท

- migration: `20260816150000_metadata_registration_form_2026_08_16`
- **ทำลายข้อมูลเดิม 16 คอลัมน์โดยไม่ backfill** เหตุผลอยู่ในหัวไฟล์ migration:
  คอลัมน์ใหม่ไม่ได้เปลี่ยนแค่ชื่อ แต่เปลี่ยนความหมายและชุดรหัส การเดาค่าให้เท่ากับ
  การกรอกแบบฟอร์มแทนหน่วยงาน ซึ่งเป็นเอกสารที่เขาต้องลงนาม
- ทุกคอลัมน์ nullable เพราะร่างบันทึกได้ทั้งที่ยังไม่ครบ — ความครบถ้วนบังคับตอนสร้าง PDF
  และตอนนำส่ง ด้วย `datasetSubmitSchema`
- เก็บ **รหัสของมาตรฐาน** ไม่ใช่ enum ที่ตั้งชื่อเอง (`"1"`, `"01"`, `"A"`, `"G0"`, `"a"`)
  ส่งต่อ DGA/DII ได้โดยไม่ต้องมีตารางแปลง และเพิ่มรหัสใหม่ได้โดยไม่ต้อง migrate
- `owner_org` เป็น FK ไปยัง `organization.organization` และ **คัดลอกมาจากหน่วยงานของคำขอ**
  ชีทเขียนว่า fixed เป็นหน่วยงานที่กรอกข้อมูล จึงไม่ใช่ช่องที่ผู้ใช้เลือก
- ตอนอนุมัติ `materialiseDataset()` คัดลอกทั้งแถวด้วย rest spread (ตัดเฉพาะ id/FK/audit)
  ไม่ไล่เขียนทีละช่อง — เพื่อให้คอลัมน์ที่เพิ่มทีหลังไม่เงียบหายตอนอนุมัติ

### ช่องที่ยังไม่มีคอลัมน์ในดีไซน์

ชีท `conditions` สั่งว่า ถ้าเป็นข้อมูลส่วนบุคคลและอนุญาตให้ส่งต่อ (ข้อ 16.1 / 16.2 / 17)
ต้อง “ระบุหน่วยที่ให้ BDI ส่งต่อ” เพิ่ม แต่ดีไซน์ 2026-08-16 ไม่มีคอลัมน์ให้ทั้งสามช่อง
จึงพักไว้ใน `additional_metadata_json` (คอลัมน์ที่ดีไซน์ระบุว่า “Optional non-core metadata”)
คีย์: `transformedRawDataRecipients` · `transformedRawDataGdxRecipients` · `aggregatedDataRecipients`
**เป็นคำถามที่ยังรอคำตอบ** ว่าจะเพิ่มคอลัมน์จริงหรือคงไว้แบบนี้

---

## 3. ช่องในแบบฟอร์ม

เรียงตามหมายเลขข้อในชีท ห้าส่วนของฟอร์มบนหน้าเว็บและใน PDF ใช้ลำดับเดียวกัน

| ข้อ | ช่อง | คอลัมน์ | ชนิด |
| --- | --- | --- | --- |
| 1.1 | ประเภทข้อมูล | `data_type` | รหัส 1 · 2 · 3 · 9 |
| 1.2 | ประเด็น (+ ระบุอื่น ๆ) | `data_topic`, `data_topic_other` | รหัส 01–07 · 99 |
| 2.1 / 2.2 | ชื่อชุดข้อมูลไทย / อังกฤษ | `title`, `name` | ข้อความ 150 |
| 3 | องค์กร | `owner_org` | FK — ระบบกรอกให้ |
| 4 / 5 | ชื่อและอีเมลผู้ติดต่อ | `maintainer`, `maintainer_email` | ข้อความ 150 / 50 |
| 6 | คำสำคัญ | `tag_string` | ข้อความ 200 คั่นด้วย `,` |
| 7 / 8 | รายละเอียด / วัตถุประสงค์ | `notes`, `objective` | ข้อความ 1,000 |
| 9.1 / 9.2 | หน่วยและค่าความถี่การปรับปรุงข้อมูลต้นทาง | `update_frequency_unit`, `update_frequency_interval` | รหัส A–U / จำนวนเต็ม |
| 9.3 | ความถี่การนำส่งเข้าระบบกลาง | `delivery_frequency` | รหัส 1–5 |
| 10 | ความละเอียดเชิงภูมิศาสตร์ | `geo_coverage` | รหัส 00–11 · 98 · 99 |
| 11 | แหล่งที่มาของข้อมูล | `data_source` | ข้อความ 200 |
| 12.1 | รูปแบบการนำส่ง (+ ระบุระบบอื่น) | `data_format`, `data_format_other` | รหัส 1–4 |
| 13.1 | หมวดหมู่ตามธรรมาภิบาลข้อมูลภาครัฐ | `data_category` | รหัส a–d |
| 13.2 | มีข้อมูลส่วนบุคคลหรือไม่ | `contains_personal_data` | Y/N |
| 13.2.1–3 | ประเภท · กลุ่มเจ้าของ · ระยะเวลาประมวลผล | `personal_data_types`, `data_subject_categories`, `personal_data_processing_period(_year/_month)` | ข้อความ / รหัส a–b / จำนวน |
| 13.3 | ระดับชั้นข้อมูล | `data_classification` | รหัส 01–05 |
| 14 | สัญญาอนุญาต | `license_id` | รหัส G0 · G2 · G5 |
| 15.1 / 15.2 | เก็บ / ส่งต่อข้อมูลดิบต้นฉบับ | `allow_original_raw_data_retention`, `allow_original_raw_data_sharing` | Y/N |
| 16.1 / 16.2 | ส่งต่อข้อมูลดิบแปลงสภาพ / ไป GDX | `allow_transformed_raw_data_sharing`, `allow_transformed_raw_data_gdx_sharing` | Y/N |
| 17 | ส่งต่อข้อมูลรวม | `allow_aggregated_data_sharing` | Y/N |
| 18 | มอบหมายให้แปลงข้อมูลส่วนบุคคลเป็นข้อมูลนิรนาม | `authorize_personal_data_anonymization` | Y/N — ถามเฉพาะเมื่อ 13.2 = มี |

---

## 4. ตารางเงื่อนไข (ชีท `conditions`)

อ่านจากซ้ายไปขวาแบบเดียวกับชีท: 13.1 → 13.2 → 13.3 → 14 → 15–17 → 18

1. **หมวด ก. ข้อมูลสาธารณะ** บังคับ 13.2 = ไม่มี, 13.3 = เปิดเผย, 14 = G0
   และบังคับ 15.1 ถึง 17 = อนุญาต ทั้งหมด ส่วนข้อ 18 ไม่ถาม
2. **ตัวเลือกของ 13.3 ขึ้นกับ 13.1** — ก. → เปิดเผย · ข. → ทั้งห้าระดับ (ถ้าเป็นข้อมูล
   ส่วนบุคคลจะตัด “เปิดเผย” ออก) · ค./ง. → ลับ · ลับมาก · ลับที่สุด
3. **ตัวเลือกของ 14 ขึ้นกับ 13.3** — เปิดเผย → G0 · เผยแพร่ภายในองค์กร → G0 หรือ G2 ·
   ลับขึ้นไป → G5
4. **ชุดข้อมูลที่เปิดเผยได้ทั้งฉบับ** (หมวด ก. หรือระดับชั้น “เปิดเผย”) ให้สิทธิ์ 15–17 ทุกข้อ
   อัตโนมัติ
5. **ระดับชั้น “เผยแพร่ภายในองค์กร” ที่ไม่มีข้อมูลส่วนบุคคล** บังคับ 16.1 · 16.2 · 17 = อนุญาต
   ส่วน 15.1 / 15.2 ผู้ใช้เลือกเอง
6. **15.2 ถามได้ต่อเมื่อ 15.1 = อนุญาต** ถ้าไม่ให้เก็บข้อมูลดิบต้นฉบับไว้ ก็ส่งต่อไม่ได้ (บังคับ N)
7. **ข้อมูลส่วนบุคคล + อนุญาตให้ส่งต่อ** (16.1 / 16.2 / 17 ข้อใดก็ตาม) ต้องระบุหน่วยงานปลายทาง
   ของข้อนั้นเพิ่ม และเปิดข้อ 18 ให้ตอบ

### บังคับที่ backend ไม่ใช่แค่ที่หน้าจอ

`normaliseMetadata()` ทำงานทุกครั้งก่อนเขียนฐานข้อมูล เติมค่าที่ถูกบังคับและล้างช่องที่
ไม่ต้องถาม ทดสอบแล้วด้วยการ PATCH ค่าที่ขัดกับชีทเข้าไปตรง ๆ (หมวดสาธารณะ + ไม่อนุญาต
ให้ส่งต่อ + ระดับชั้นลับที่สุด) ระบบเก็บเป็น เปิดเผย · G0 · อนุญาตทุกข้อ ตามชีท

### ค่าที่ถูกบังคับแล้วเงื่อนไขคลายออก

หน้าเว็บจะ **ล้างค่านั้นทิ้ง** ไม่ใช่ปล่อยค้างไว้ (เช่น เลือกหมวด ก. แล้วเปลี่ยนเป็น ค.)
เอกสารนี้คือหนังสือให้สิทธิ์ที่หน่วยงานลงนาม คำตอบที่ผู้ใช้ไม่เคยเลือกเองไม่ควรติดไปด้วย

---

## 5. สมมติฐานและคำถามที่ยังไม่มีคำตอบ

| # | เรื่อง | สิ่งที่ทำไปก่อน |
| --- | --- | --- |
| 1 | ชีท `A4` หมายเหตุว่า “ใช้ภายใน → เผยแพร่ภายในองค์กร, ลับ, ลับมาก, ลับที่สุด” (ไม่มี “เปิดเผย”) แต่ชีท `conditions` แถวที่ 3 ให้หมวด ข. เลือก “เปิดเผย” ได้ | เดินตาม `conditions` ตามที่การ์ดระบุว่าเป็นชีทที่บอกเงื่อนไข |
| 2 | ชีท `สัญญาอนุญาต` ตารางท้ายชีทบอกว่า เปิดเผย → G0/G2 และ ใช้ภายใน → G2/G5 ซึ่งไม่ตรงกับ `conditions` (เปิดเผย → G0, ใช้ภายใน → G0/G2) | เดินตาม `conditions` ด้วยเหตุผลเดียวกัน |
| 3 | `update_frequency_interval` ถูกมาร์ก Required แต่หน่วย “ตามเวลาจริง / ไม่มีการปรับปรุง / ไม่ทราบ” ไม่มีค่าความถี่ที่ถูกต้อง | ซ่อนช่องและไม่บังคับกรอกเมื่อหน่วยเป็น R · O · U |
| 4 | `geo_coverage` รหัส 99 เขียนว่า “อื่น ๆ ระบุ…” แต่ดีไซน์ไม่มีคอลัมน์ให้ระบุ (ต่างจาก `data_topic` ที่มี `data_topic_other`) | ไม่เพิ่มคอลัมน์เอง ป้ายจึงเป็น “อื่น ๆ” เฉย ๆ |
| 5 | หน่วยงานปลายทางที่ให้ BDI ส่งต่อ (ข้อ 16–17 กรณีข้อมูลส่วนบุคคล) ไม่มีคอลัมน์ | เก็บใน `additional_metadata_json` — ดู §2 |
| 6 | การยอมรับเงื่อนไขการนำส่งข้อมูลควรลงตาราง `legal.legal_acceptance` แต่ตารางนั้นบังคับทั้ง `legal_document_version_id` และ `review_task_id` ซึ่งตอนกรอกร่างยังไม่มีทั้งคู่ และเอกสารกฎหมายที่ seed ไว้ยังเป็น DRAFT | เก็บเวลาที่ผู้ใช้ติ๊กไว้ใน `additional_metadata_json` และบังคับต้องติ๊กก่อนสร้าง PDF |

---

## 6. ไฟล์ที่เกี่ยวข้อง

| ไฟล์ | หน้าที่ |
| --- | --- |
| `backend/src/lib/dataset.ts` | รหัส · ป้ายไทย · `metadataRules()` · `normaliseMetadata()` · zod ร่าง/นำส่ง |
| `backend/src/routes/dataset-requests.ts` | บันทึกร่าง (merge + normalise ทุกครั้ง) · สร้าง PDF · นำส่ง · คัดลอกไป `dataset_metadata` |
| `backend/src/lib/pdf.ts` | แบบฟอร์ม A4 ห้าส่วน — ช่องที่เงื่อนไขสั่งซ่อนจะไม่ถูกพิมพ์ |
| `frontend/lib/dataset-form.ts` | **สำเนาของกฎฝั่ง backend** เพื่อให้หน้าจอตอบสนองทันที — แก้ไฟล์หนึ่งต้องแก้อีกไฟล์ |
| `frontend/app/datasets/[id]/edit/page.tsx` | ฟอร์มห้าส่วน + แถบความคืบหน้า |
| `frontend/components/dataset/DetailView.tsx` | หน้ารายละเอียดทั้งฝั่งหน่วยงานและฝั่ง BDI |
